# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage

# env
.env
.env*.local
.env.local

# misc
.DS_Store
*.pem
*.log
npm-debug.log*
.vercel
*.tsbuildinfo

# WatchVerdict eval framework — ephemeral run outputs (baseline is kept)
/evaluation-results/runs/
/evaluation-results/latest.json
/eval/gold/regression.json

# Playwright transient artifacts
/test-results/

# Search-audit run artifacts (live responses, probe outputs) — evidence stays
# local; only the frozen corpus + oracle are tracked, under scripts/searchAudit.
/artifacts/
