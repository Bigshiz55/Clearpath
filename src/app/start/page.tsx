import { FreshStart } from '@/components/FreshStart';

/** Brand-new clean-slate link — mints a fresh anonymous guest and opens the
 *  Taste Quiz with zero history. Share it so each person starts from scratch. */
export default function StartPage() {
  return <FreshStart />;
}
