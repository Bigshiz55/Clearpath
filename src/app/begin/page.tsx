import { FreshStart } from '@/components/FreshStart';

/** Another brand-new clean-slate link — fresh anonymous guest, straight into
 *  the Taste Quiz with zero history. Same flow as /fresh and /start. */
export default function BeginPage() {
  return <FreshStart />;
}
