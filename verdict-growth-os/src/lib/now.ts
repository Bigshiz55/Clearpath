/**
 * A fixed "now" for the v1 demo so the seeded briefing, deadlines, and urgency
 * calculations are deterministic and reproducible. When real adapters land,
 * this is replaced by the request time.
 */
export const DEMO_NOW = "2026-07-24T13:00:00.000Z";
